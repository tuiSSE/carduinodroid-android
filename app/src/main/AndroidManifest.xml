<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swp.tuilmenau.carduinodroid2">

    <uses-sdk android:minSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.swp.tuilmenau.carduinodroid_android.CarduinodroidApplication">

        <activity
            android:name="com.swp.tuilmenau.carduinodroid_android.StatusActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.swp.tuilmenau.carduinodroid_android.SerialService"
            android:enabled="true"
            android:exported="true">
        </service>
    </application>

</manifest>
