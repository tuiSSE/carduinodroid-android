<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:theme="@style/PreferencesTheme">
<!--
    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="@string/pref_description_social_recommendations"
        android:title="@string/pref_title_social_recommendations" />
-->
    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
<!--
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_display_name"
        android:inputType="textCapWords"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_display_name" />
-->

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_control_mode"
        android:entryValues="@array/pref_control_mode_values"
        android:key="@string/pref_key_control_mode"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_control_mode"
        android:icon="@drawable/pref_icon_control_mode"/>

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_debug_view"
        android:entryValues="@array/pref_debug_view_values"
        android:key="@string/pref_key_debug_view"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_debug_view"
        android:icon="@drawable/pref_icon_debug"/>


    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_reset_battery"
        android:entryValues="@array/pref_reset_battery_values"
        android:key="@string/pref_key_reset_battery"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_reset_battery"
        android:icon="@drawable/pref_icon_batt_reset"/>

    <ListPreference
        android:defaultValue="60000"
        android:entries="@array/pref_screensaver"
        android:entryValues="@array/pref_screensaver_values"
        android:key="@string/pref_key_screensaver"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_screensaver"
        android:icon="@drawable/pref_icon_screensaver"/>


</PreferenceScreen>
